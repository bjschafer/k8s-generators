apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: kube-system
  name: kured
rules:
  - apiGroups:
      - apps
    resources:
      - daemonsets
    resourceNames:
      - kured
    verbs:
      - update
