#!/usr/bin/env bash
#MISE description="Remove generated dist files (preserving ArgoCD sources and sealed secrets)"

set -euo pipefail

find dist -type f ! \( -name '.argocd-source-*.yaml' -o -name 'sealedsecret.*.yaml' \) -delete
find dist -type d -empty -delete
