apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-app-config
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: cephfs
  volumeMode: Filesystem
