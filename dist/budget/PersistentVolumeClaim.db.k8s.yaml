apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db
  namespace: budget
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
