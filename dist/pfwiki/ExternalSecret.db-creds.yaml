apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: db-creds
  namespace: pfwiki
spec:
  data:
    - remoteRef:
        key: a34911fa-e45d-40e6-815b-b47e018222a2
      secretKey: MARIADB_ROOT_PASSWORD
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden
  target:
    name: db-creds
