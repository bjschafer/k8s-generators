apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: tautulli
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
