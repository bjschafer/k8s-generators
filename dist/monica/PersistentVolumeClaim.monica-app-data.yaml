apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monica-app-data
  namespace: monica
spec:
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
