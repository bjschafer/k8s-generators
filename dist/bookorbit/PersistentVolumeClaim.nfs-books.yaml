apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-books
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1024Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: bookorbit-nfs-books
