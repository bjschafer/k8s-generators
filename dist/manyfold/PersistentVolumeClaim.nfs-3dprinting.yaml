apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-3dprinting
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 51200Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: nfs-3dprinting
