apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: manyfold-secrets
  namespace: manyfold
spec:
  data:
    - remoteRef:
        key: b8f60e2c-1436-4365-9c04-b3cc001b52fd
      secretKey: SECRET_KEY_BASE
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden
  target:
    name: manyfold-secrets
