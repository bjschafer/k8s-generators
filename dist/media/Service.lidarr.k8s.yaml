apiVersion: v1
kind: Service
metadata:
  name: lidarr
  namespace: media
spec:
  externalIPs: []
  ports:
    - name: http
      port: 8686
      targetPort: 8686
  selector:
    cdk8s.io/metadata.addr: lidarr-lidarr-deployment-c8c0031b
  type: ClusterIP
