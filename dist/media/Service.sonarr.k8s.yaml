apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media
spec:
  externalIPs: []
  ports:
    - name: http
      port: 8989
      targetPort: 8989
  selector:
    cdk8s.io/metadata.addr: sonarr-sonarr-deployment-c8de73e9
  type: ClusterIP
