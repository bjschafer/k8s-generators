apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resilio-sync-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
