apiVersion: v1
kind: Service
metadata:
  name: frr-k8s-webhook-service
  namespace: metallb-system
spec:
  ports:
    - port: 443
      targetPort: webhook
  selector:
    app.kubernetes.io/component: statuscleaner
