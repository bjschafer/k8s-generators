apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
