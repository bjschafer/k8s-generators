apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: paperless
    component: web
  name: data
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
