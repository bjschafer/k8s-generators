apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: paperless-ai
  name: paperless-ai-data
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
