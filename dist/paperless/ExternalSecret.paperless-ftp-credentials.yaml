apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-ftp-credentials
  namespace: paperless
spec:
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: paperless-ftp-password-generator
  refreshInterval: "0"
  target:
    name: paperless-ftp-credentials
