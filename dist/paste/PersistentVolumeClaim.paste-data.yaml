apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paste-data
  namespace: paste
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Gi
  storageClassName: cephfs
  volumeMode: Filesystem
