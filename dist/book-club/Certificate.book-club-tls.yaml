apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: book-club-tls
  namespace: book-club
spec:
  dnsNames:
    - book-club.cmdcentral.xyz
    - book-club.whizkid.dev
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: book-club-tls
