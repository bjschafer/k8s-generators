apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: netboot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
