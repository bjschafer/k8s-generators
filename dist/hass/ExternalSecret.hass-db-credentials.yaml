apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hass-db-credentials
  namespace: hass
spec:
  data:
    - remoteRef:
        key: hass-db-credentials
        property: password
      secretKey: password
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes
  target:
    name: hass-db-credentials
