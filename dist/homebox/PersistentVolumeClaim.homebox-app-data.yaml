apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homebox-app-data
  namespace: homebox
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
