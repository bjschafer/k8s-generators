apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-server
  namespace: argocd
spec:
  dnsNames:
    - argo.cmdcentral.xyz
    - argocd.cmdcentral.xyz
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: argocd-server
