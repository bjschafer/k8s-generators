apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filestash-app-state
  namespace: filestash
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: cephfs
  volumeMode: Filesystem
