apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bambuddy-db-credentials
  namespace: bambuddy
spec:
  data:
    - remoteRef:
        key: bambuddy-db-credentials
        property: password
      secretKey: password
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes
  target:
    name: bambuddy-db-credentials
