apiVersion: v1
kind: Service
metadata:
  labels:
    app: unifi-exporter
  name: unifi-exporter
  namespace: metrics-exporters
spec:
  externalIPs: []
  ports:
    - name: http
      port: 9130
      targetPort: 9130
  selector:
    cdk8s.io/metadata.addr: unifi-unifi-app-unifi-app-deployment-c85aea89
  type: ClusterIP
