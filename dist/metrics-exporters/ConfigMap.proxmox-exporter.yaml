apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: proxmox-exporter
  name: proxmox-exporter
  namespace: metrics-exporters
data:
  pve.yml: |
    default:
      verify_ssl: true
immutable: false
