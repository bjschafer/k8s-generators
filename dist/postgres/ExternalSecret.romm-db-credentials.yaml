apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: romm-db-credentials
  namespace: postgres
spec:
  data:
    - remoteRef:
        key: efb8a53a-dfd4-4f3a-b2e2-b39200423731
      secretKey: password
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden
  target:
    name: romm-db-credentials
