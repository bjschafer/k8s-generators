apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafanareader-db-credentials
  namespace: postgres
spec:
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: grafanareader-pg-password-generator
  refreshInterval: "0"
  target:
    name: grafanareader-db-credentials
    template:
      data:
        password: "{{ .password }}"
        username: grafanareader
      metadata:
        labels:
          cnpg.io/reload: "true"
      type: kubernetes.io/basic-auth
