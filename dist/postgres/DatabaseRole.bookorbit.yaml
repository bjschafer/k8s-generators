apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: bookorbit
  namespace: postgres
spec:
  cluster:
    name: immich
  comment: BookOrbit database owner
  login: true
  name: bookorbit
  passwordSecret:
    name: bookorbit-db-credentials
