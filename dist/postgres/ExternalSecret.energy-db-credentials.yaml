apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: energy-db-credentials
  namespace: postgres
spec:
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: energy-pg-password-generator
  refreshInterval: "0"
  target:
    name: energy-db-credentials
    template:
      data:
        password: "{{ .password }}"
        username: energy
      metadata:
        labels:
          cnpg.io/reload: "true"
      type: kubernetes.io/basic-auth
