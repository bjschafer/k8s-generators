apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hass-db-credentials
  namespace: postgres
spec:
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: hass-pg-password-generator
  refreshInterval: "0"
  target:
    name: hass-db-credentials
    template:
      data:
        password: "{{ .password }}"
        username: hass
      metadata:
        labels:
          cnpg.io/reload: "true"
      type: kubernetes.io/basic-auth
