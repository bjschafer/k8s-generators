apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: immich-pg16-db
  namespace: postgres
spec:
  cluster:
    name: immich-pg16
  databaseReclaimPolicy: retain
  extensions:
    - name: vector
    - name: vchord
  name: immich
  owner: immich
