apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-dump
  namespace: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-rbd
  volumeMode: Filesystem
